

{{title}}
<div class="jumbotron text-center">

	<div class="background-image" style="background-color: #DC143C">
	 
	<img src="/img/radio_day.jpg" class="img-responsive"></img>
	<div class="caption" style="color: #FFF">
    <h1 >Mongo Scraper</h1>
    <p>NPR News</p>
   </div>
	<br>
	<br>
	</div>
</div>
	<div class="container">
		
	<div class="panel panel-danger">
		{{#each myitems as |article|}}
	
		    <div class="panel-heading">
			    <h1 class="panel-title" name="title" href ="{{article.link}}"> <a href ="{{article.link}}">{{article.title}}</a></h1>
			    </div>
			    <div class="panel-body" name="link" ><a href ="{{article.link}}" ><h3> {{article.title}} </h3></a> 
			    <input name="article_id" value="{{article.id}}" type="hidden" />
			    <button type="submit" class="alert btn btn-danger" href="/delete">Delete</button>
		    </div>
		    
		{{/each}}
	    <br>
      </div>
     </div>  
     

